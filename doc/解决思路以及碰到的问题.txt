一、准备工作
    1、将数据按照预定义格式存储到文本中，程序读取文件并解析数据。

二、数据格式
    1、每个大班下的所有小班组成一个单向链表
    2、使用一个数组，保存每个链表头指针
    3、将时间和实验室数据组成二维数组；行代表时间，列代表实验室
    
三、思路
    1、依次取出小班链表，进行排课
    2、如果某一大班的小班不能同时在一天时间内上课，则回溯寻找其他合适的大班

四、问题
    1、大班与大班之间的衔接问题--这个应该不难，自己没想明白
    2、大班的回溯--现在只做了大班内小班之间的回溯，大班与大班之间的查找最优排课方案没有做.
